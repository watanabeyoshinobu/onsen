<div class="blog-card">
  <div class="blog-header">
    <h1 class="blog-title"><%= @blog.title %></h1>
    <div class="blog-meta">
      <span class="blog-author">
        <i class="fa fa-user"></i> <%= @blog.user.name %>
      </span>

      <% if user_signed_in? && @blog.user != current_user %>
        <span class="blog-follow-area">
          <% if current_user.following?(@blog.user) %>
            <%= form_for(current_user.relationships.find_by(follow_id: @blog.user.id), html: { method: :delete, class: "inline-form" }) do |f| %>
              <%= hidden_field_tag :follow_id, @blog.user.id %>
              <%= f.button type: "submit", class: "blog-btn-unfollow" do %>
                <i class="fa fa-check"></i> フォロー中
              <% end %>
            <% end %>
          <% else %>
            <%= form_for(current_user.relationships.build, html: { class: "inline-form" }) do |f| %>
              <%= hidden_field_tag :follow_id, @blog.user.id %>
              <%= f.button type: "submit", class: "blog-btn-follow" do %>
                <i class="fa fa-user-plus"></i> フォロー
              <% end %>
            <% end %>
          <% end %>
        </span>
      <% end %>
      <span class="blog-date">
        <%= @blog.created_at.strftime('%Y/%m/%d') %>
      </span>
    </div>
  </div>

  <div class="blog-body">
    <% if @blog.image.present? %>
      <div class="blog-image-container">
        <%= image_tag @blog.image.to_s, class: 'blog-image' %>
      </div>
    <% end %>
    
    <div class="blog-text">
      <%= simple_format(@blog.body) %>
    </div>
  </div>

  <div class="blog-footer">
    <div class="blog-actions">
      <% if user_signed_in? %>
        <div class="favorite-button-component"
            data-blog-id="<%= @blog.id %>"
            data-favorited="<%= @blog.favorited_by?(current_user) %>"
            data-count="<%= @blog.favorites.count %>">
        </div>
      <% else %>
        <div class="favorite-btn">
          <i class="fa fa-heart-o" aria-hidden="true"></i>
          <span><%= @blog.favorites.count %></span>
        </div>
      <% end %>
    </div>
  </div>
</div>